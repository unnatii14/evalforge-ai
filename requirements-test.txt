# Minimal dependency set for running the test suite in CI.
# The heavy ML libraries (ragas, deepeval, langchain, faiss, streamlit, torch)
# are intentionally omitted: the suite runs fully offline and exercises the
# evaluators' fallback paths, so it never imports them.
fastapi>=0.115
pydantic>=2.7
pydantic-settings>=2.3
pandas>=2.2
python-multipart>=0.0.9
reportlab>=4.2
ollama>=0.3
httpx>=0.27
pytest>=8.0
